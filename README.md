# Python-Calculator-with-Error-Handling
This is a simple command-line calculator built in Python that performs basic arithmetic operations: addition, subtraction, multiplication, and division. The program takes user input for two numbers and an operator, then displays the result based on the selected operation.
